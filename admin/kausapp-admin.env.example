# Copy to kausapp-admin.env and fill in CF_API_TOKEN, then keep it private (chmod 600).
# Token needs: Account -> Workers KV Storage -> Read  (Edit if you want the Delete button to work).
CF_API_TOKEN=
CF_ACCOUNT_ID=30951f274c5ebe109b224b63bc6de688
REPORTS_KV_ID=ce3a8a24c91b4797a3dedf4d78e3fb7c
# Bind to the droplet's Tailscale IP so the admin is reachable ONLY from the tailnet.
BIND_HOST=100.99.99.75
BIND_PORT=8080
